import 'package:get_it/get_it.dart';

final sl = GetIt.instance;

Future<void> init() async {
  // Register services
  // sl.registerLazySingleton(() => AnyCoreServices());

  // Register repositories
  // sl.registerLazySingleton(() => AnyCoreRepositories());

  // Register bloc
  // sl.registerFactory(() => AnyCoreBlocs());
}
